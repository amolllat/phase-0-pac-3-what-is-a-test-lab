const name = "Susan";
const height = 39;
// function result(height) {
// if (height > 0 && height > 40) {
//     return "Height must be greater than 0 and less than 40";
// }}
const message = `${name} is ${height} inches tall`;

module.exports = {name, height, message};
